<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzccaidp.mapper.datasource.TablePermissionMapper">


    <insert id="batchInsert"
            parameterType="java.util.List">
        INSERT INTO table_permission(id, table_id, datasource_id, principal_id, principal_type, create_time, create_user)
        VALUES
        <foreach collection="dataList" item="item" separator=",">
            (#{item.id}, #{item.tableId}, #{item.datasourceId}, #{item.principalId}, #{item.principalType}, #{item.createTime}, #{item.createUser})
        </foreach>
    </insert>
</mapper>
