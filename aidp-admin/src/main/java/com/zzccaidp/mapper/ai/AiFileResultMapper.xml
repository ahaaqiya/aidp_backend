<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzccaidp.mapper.ai.AiFileResultMapper">
    <select id="selectAiFileByUser" resultType="com.zzccaidp.dao.ai.AiFileResultDO">
        select
        file_id,
        file_extraction_result,
        file_extraction_template,
        file_parsing_result
        from
        zzccaidp_ai_file_result
        where
        file_id=#{fileId}
    </select>
    <delete id="deleteByPrimaryKeys">
        delete from zzccaidp_ai_file_result where file_id in
        <foreach item="item" index="index" collection="fileIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <select id="listAiFileHistoryResult" resultType="com.zzccaidp.dao.ai.AiFileResultDO">
        select
        file_id,
        file_extraction_result,
        file_extraction_template,
        file_parsing_result
        from zzccaidp_ai_file_result where file_id in
        <foreach item="item" index="index" collection="fileIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

</mapper>