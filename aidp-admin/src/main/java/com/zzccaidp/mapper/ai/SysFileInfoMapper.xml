<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzccaidp.mapper.ai.SysFileInfoMapper">
    <select id="selectAiFileByUser" resultType="com.zzccaidp.dao.ai.SysFileInfoDO">
        select
            file_id,
            file_type,
            file_name,
            file_path,
            file_business_typs,
            file_storage_typs,
            create_time,
            create_user,
            update_time,
            update_user,
            is_delete,
            file_size,
            scratch_file
        from
            zzccaidp_sys_file
        where
            file_business_typs=#{aiFile}
        and create_user = #{userId}
        order by create_time desc
    </select>

    <!-- 新增的批量查询 SQL 语句 -->
    <select id="selectByPrimaryKeys" resultType="com.zzccaidp.dao.ai.SysFileInfoDO">
        SELECT * FROM zzccaidp_sys_file
        WHERE file_id IN
        <foreach item="item" index="index" collection="fileIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 批量删除 SQL 语句 -->
    <delete id="deleteByPrimaryKeys">
        DELETE FROM zzccaidp_sys_file
        WHERE file_id IN
        <foreach item="item" index="index" collection="fileIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <select id="selectScratchFileByUserName" resultType="java.lang.String">
        select file_id FROM zzccaidp_sys_file
        where create_user=#{userName}
        and file_business_typs='aiFile'
        and scratch_file = 1
    </select>
    <update id="updateFileInfo">
        UPDATE zzccaidp_sys_file SET scratch_file =#{scratchFile} WHERE file_id=#{fileId}
    </update>
</mapper>