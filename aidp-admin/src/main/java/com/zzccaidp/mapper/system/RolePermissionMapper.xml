<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzccaidp.mapper.system.RolePermissionMapper">
    <delete id="deleteByIdList">
        delete from zzccaidp_role_permission where id in
        <foreach collection="idList" item="id" close=")" open="(" separator=",">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByPerIdList">
        delete from zzccaidp_role_permission where permission_id in
        <foreach collection="perIdList" item="id" close=")" open="(" separator=",">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByRoleIdList">
        delete from zzccaidp_role_permission where role_id in
        <foreach collection="roledList" item="id" close=")" open="(" separator=",">
            #{id}
        </foreach>
    </delete>

    <insert id="batchInsterRolePermission">
        insert into zzccaidp_role_permission(id, gmt_create, gmt_create_user, gmt_modified, gmt_modified_user,
        role_id, permission_id,status) values
        <foreach collection="rolePermissionDOList" separator="," item="item">
            (#{item.id},#{item.gmtCreate},#{item.gmtCreateUser},#{item.gmtModified},#{item.gmtModifiedUser},#{item.roleId},#{item.permissionId},#{item.status})
        </foreach>
    </insert>
</mapper>
